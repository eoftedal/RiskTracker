<!DOCTYPE html>
<html>
<head>
  <meta charset='utf-8'>
  <title>Risk manager<%= yield(:title) %></title>
  <%= stylesheet_link_tag "scaffold", "style" %>
  <%= yield(:pre_head) %>
  <%= javascript_include_tag :defaults, "jquery-ui", "jquery.tablesorter.min", "application" %>
  <%= yield(:head) %>
  <%= csrf_meta_tag %>
</head>
<body>
  <div id="container">
    <header class="main">

        <h1>
        <div class="userinfo">
            Not logged in
        </div>
                <a href=".">Risk tracker</a>
        </h1>
    </header>
       <nav>
                <ul>
                      <li><a href="/session/new">Login</a></li>
                </ul>
        </nav>

        <div id="main">
            <%= yield %>
      <div style="clear:both;"></div>
        </div>

    <footer>

    </footer>
  </div>
</body>
</html>
