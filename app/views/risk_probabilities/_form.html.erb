<%= form_for([current_risk_configuration, @risk_probability]) do |f| %>
  <% if @risk_probability.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@risk_probability.errors.count, "error") %> prohibited this probability from being saved:</h2>

      <ul>
      <% @risk_probability.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>
    
  <div class="field">
    <%= f.label :weight %>
    <%= f.text_field :weight %>
  </div>
  <div class="actions">
    <%= f.submit :class => "btn btn-primary" %>
  </div>
<% end %>
