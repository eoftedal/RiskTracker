<%= form_for(@risk_configuration) do |f| %>
  <% if @risk_configuration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@risk_configuration.errors.count, "error") %> prohibited this risk_configuration from being saved:</h2>

      <ul>
      <% @risk_configuration.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>


  <div class="actions">
    <%= f.submit :class => "btn btn-primary" %>
  </div>
<% end %>
