<%= form_for(@project) do |f| %>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :risk_configuration %><br />
    <%= select("project", "risk_configuration_id", RiskConfiguration.all.sort_by{|c| c.name }.collect{|c| [c.name, c.id] } ) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
